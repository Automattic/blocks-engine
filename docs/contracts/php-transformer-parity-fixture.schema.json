{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Automattic/blocks-engine/docs/contracts/php-transformer-parity-fixture.schema.json",
  "title": "Blocks Engine PHP Transformer parity fixture",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "name", "description", "operation", "input", "expect"],
  "properties": {
    "schema": {
      "const": "blocks-engine/php-transformer/parity-fixture/v1"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "source_reference": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "repo": { "type": "string" },
        "path": { "type": "string" },
        "notes": { "type": "string" }
      }
    },
    "legacy_comparison": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" },
        "skip": { "type": "boolean" },
        "reason": { "type": "string" },
        "repo": {
          "type": "string",
          "enum": ["html-to-blocks-converter", "block-format-bridge", "block-artifact-compiler"]
        },
        "path": { "type": "string", "minLength": 1 },
        "bootstrap": { "type": "string", "minLength": 1 },
        "callable": { "type": "string", "minLength": 1 },
        "paths": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        }
      }
    },
    "operation": {
      "type": "string",
      "enum": ["html_transformer.transform", "format_bridge.normalize", "format_bridge.convert", "artifact_compiler.compile"]
    },
    "input": {
      "type": "object"
    },
    "expect": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "assert"],
        "properties": {
          "path": { "type": "string", "minLength": 1 },
          "assert": {
            "type": "string",
            "enum": ["equals", "contains", "not_contains", "count"]
          },
          "value": {},
          "count": { "type": "integer", "minimum": 0 }
        },
        "allOf": [
          {
            "if": { "properties": { "assert": { "const": "count" } } },
            "then": { "required": ["count"] },
            "else": { "required": ["value"] }
          }
        ]
      }
    }
  }
}
